;
(function(win) {

  var site_namespace = win.__site_namespace__;

  var namespace = site_namespace + '.Modules.Header';

  var logger = Logger.get(namespace);

  var Header = Namespace(namespace);

  logger.info('Init');

}(window));
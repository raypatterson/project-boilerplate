;
(function(win) {

  var site_namespace = win.__site_namespace__;

  var namespace = site_namespace + '.Modules.Footer';

  var logger = Logger.get(namespace);

  var Footer = Namespace(namespace);

  logger.info('Init');

}(window));